using WateryTart.MusicAssistant.Messages;
using WateryTart.MusicAssistant.Responses;

namespace WateryTart.MusicAssistant.WebSocketExtensions
{
    public static partial class WebsocketClientExtensions
    {
        /*public static async Task<ArtistResponse> GetConfigCoreAsync(this IWsClient c,
            string provider_instance_id_or_domain)
        {
            return await SendAsync<ArtistResponse>(c,
                IdAndProvider(Commands.ArtistGet, artistid, provider_instance_id_or_domain));
        }*/
    }

    
}
